# Production build env (loaded automatically by Vite when `vite build` runs).
# Public URL — safe to commit. R2 upload credentials stay in `.env`.
VITE_DATA_BASE_URL=https://budget-assets.bettergov.ph
